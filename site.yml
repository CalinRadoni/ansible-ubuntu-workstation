---

- name: 'Ansible for Ubuntu configuration'
  hosts: all
  become_method: sudo
  become_user: '{{ local_user_name | mandatory }}'
  become: yes

  roles:
    # - role: setuser
    #   vars:
    #     v_user:
    #       name: '{{ local_user_name }}'
    #       groups: sudo

    # - role: common
    #   when: (common_apps is defined) or (group_apps is defined)

    # - role: firewall

    # - role: git

    # - role: python

    # - role: vim

    # - role: keyboard
    #   when: kbd_profiles is defined

    # - role: dconf

    # - role: ruby_bundler

    # - role: nodejs
    #   vars:
    #     nodejs_version: 16

    # - role: vscode

    # - role: teams

    # - role: kicad
    #   vars:
    #     kicad_version: 5.1
